{
  "name": "xpath-arena",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/client",
    "dev:server": "npm run dev -w apps/server",
    "dev:all": "npm run dev:server & npm run dev",
    "build": "npm run build -w packages/shared && npm run build -w packages/game-engine && npm run build -w packages/challenge-engine && npm run build -w apps/client && npm run build -w apps/server",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/jsdom": "^30.0.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
